import { keyframes, style } from '@angular/animations';
export const bounceOutLeft=[
    style({opacity:1, transform:'translate3d(20px, 0, 0)',offset:.2}),
    style({opacity:'none', transform:'none',offset:1})

]

